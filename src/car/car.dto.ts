/*
 * DTO: Data Transfer object. It's an object that defines how data will be sent
 * over the network. It's an object that defines how data will be sent over the
 * network.
 */

export class CarDto {
    readonly id: number;
    readonly brand: string;
    readonly color: string;
    readonly models: string;
}
